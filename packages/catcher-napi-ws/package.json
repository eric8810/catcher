{
  "name": "@eric8810/catcher-napi-ws",
  "version": "0.2.2",
  "description": "catcher WebSocket native addon for Node.js via napi-rs",
  "main": "client.js",
  "types": "client.d.ts",
  "files": ["client.js", "client.d.ts", "index.js", "index.d.ts", "*.node", "npm/"],
  "napi": {
    "name": "catcher-napi-ws",
    "triples": { "defaults": true }
  },
  "scripts": {
    "build": "napi build --platform --release && napi artifacts",
    "build-debug": "cargo build",
    "prepublishOnly": "node -e \"const fs=require('fs');if(!fs.existsSync('index.js')){console.error('Run npm run build first (requires Rust)');process.exit(1)}\""
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  },
  "license": "MIT"
}
